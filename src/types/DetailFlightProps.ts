/**
 * Representa a tipagem para a props que devem
 * estar presenta no componente `DetailFlight`.
 */
export type DetailFlightProps = {
    title: string;
    content: string | number;
    balance?: boolean;
};
